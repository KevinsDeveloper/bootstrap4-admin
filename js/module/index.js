define(['jquery', 'bootstrap', 'fast', 'template'], function ($, undefined, Fast, template) {
    var Controller = {
        index: function () {
                
        }
    }
    return Controller;
});